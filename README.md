# Appointment Board - Full Stack Task
Backend: cd Backend/appointment-board -> python -m uvicorn main:app --port 8001 --reload
Frontend: Open Frontend/index.html
Features: Add, Edit with STATUS fix, Complete, Cancel, Filter by date/status, Time clash check, Red for cancelled
Fixed Bug: Now edit status changes both top form and bottom card